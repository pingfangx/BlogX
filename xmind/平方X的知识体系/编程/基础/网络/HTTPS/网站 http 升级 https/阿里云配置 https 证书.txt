管理控制台 > 产品与服务 > 安全（云盾） > SSL 证书（应用安全）

购买 > Symantec > 1 个域名 > 免费型DV SSL

# DNS验证方式
一开始配置 @ TXT  
> 提示 已经存在主机名为@的CNAME记录，请先删除此记录，否则TXT记录将不会生效!  

查看帮助，主机记录写的 @，而实际它说可以填主机记录 _dnsauth

于是改为 _dnsauth TXT，还是验证失效，刷新了一下，正想撤销申请，改为 文件验证方式  
然后就认证通过了。

因此配置时

    主机名 _dnsauth
    解析类型 TXT
    对应值 填那个认证