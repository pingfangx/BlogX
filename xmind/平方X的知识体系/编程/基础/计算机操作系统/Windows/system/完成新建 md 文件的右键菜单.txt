# 选择右键菜单位置
new 菜单或者自定义

# 自定义右键菜单
* 手动添加
* dos 命令
* 导入 reg

# 传当前到命令
    rem %v 表示传当前目录，网上说的 %1 测试无效，查看 git 是使用 %v
    rem %%v 是 bat 需要转义
    rem \" 是引号写入注册表时需要转义
    rem 转义后最后写入注册表的值为 "<sub_cmd_cmd>" "%v"
    
# 接收目录并创建 md 文件
使用 copy 命令